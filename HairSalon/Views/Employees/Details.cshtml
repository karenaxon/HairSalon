@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model HairSalon.Models.Employee;

<h2>Employee Details</h2>
<hr />

<h3>@Html.Label("Hair Stylist:") @Html.DisplayFor(model => model.Name)<h3>
<h3>@Html.Label("Employee ID:") @Html.DisplayFor(model => model.EmployeeId)<h3>

<ul>
@if (@Model.Clients.Count == 0)
{
  <h4>No clients have been added.</h4>
}
</ul>

<h4 class="mt-4">Clients:</h4>
@foreach (Client client in @Model.Clients)
{
  <li>@client.Name</li>
}


@* <p>@Html.ActionLink("Edit Employee", "Edit", new { id = Model.CategoryId })</p> *@
<p>@Html.ActionLink("Back to employees list", "Index")</p>
@* <p>@Html.ActionLink("Delete Employee", "Delete", new { id = Model.CategoryId })</p> *@
